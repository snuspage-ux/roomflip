import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { SessionProvider } from "@/components/session-provider";
import "./globals.css";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: "RoomAI - AI Interior Design | Redesign Any Room Instantly",
  description: "Upload a photo of your room and AI redesigns it in 15+ stunning styles. Modern, Japanese, Scandinavian, Industrial and more. Professional interior design in seconds.",
  keywords: ["AI interior design", "room redesign", "AI room makeover", "interior design AI", "room decorator"],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="dark">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <SessionProvider>{children}</SessionProvider>
      </body>
    </html>
  );
}
